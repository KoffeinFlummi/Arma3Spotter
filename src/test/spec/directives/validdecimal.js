'use strict';

describe('Directive: validDecimal', function () {
/*
  // load the directive's module
  beforeEach(module('arma3SpotterApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<valid-decimal></valid-decimal>');
    element = $compile(element)(scope);
    expect(element.text()).toBe('this is the validDecimal directive');
  }));*/
});
